/** ISR / Data Cache revalidation for public CMS fetches (seconds). */
export const CMS_REVALIDATE_SECONDS = 30;

/** In-memory dedupe TTL per Node instance (milliseconds), aligned with ISR window. */
export const CMS_MEMORY_TTL_MS = 30_000;
